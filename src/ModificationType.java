/**
 * Enum for the different types of modifications that can be made to a cache entry.
 * 
 * @author Zijie Huang
 */
public enum ModificationType {
    INCREMENT_REF_COUNT,
    DECREMENT_REF_COUNT,
    RESET_LRU_COUNT,
}
